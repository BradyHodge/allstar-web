; ─────────────────────────────────────────────────────────
;  AllStar HTTP-based node registration
;  NODE_NUMBER and NODE_PASSWORD are injected at startup.
; ─────────────────────────────────────────────────────────

[general]

[registrations]
register => ${NODE_NUMBER}:${NODE_PASSWORD}@register.allstarlink.org
